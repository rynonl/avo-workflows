# frozen_string_literal: true

# Configure Avo Workflows
Avo::Workflows.configure do |config|
  # Configure the user model for workflow assignments
  # config.user_class = "User"
  
  # Enable or disable workflows globally
  # config.enabled = true
  
  # Optional: Configure custom workflow execution model
  # config.workflow_execution_class = "Avo::Workflows::WorkflowExecution"
end